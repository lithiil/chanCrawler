# frozen_string_literal: true

RSpec.describe ChanCrawlerGem do
  it 'has a version number' do
    expect(ChanCrawlerGem::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
